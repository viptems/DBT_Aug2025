# Practise Assigment - Focus on ERROR's 
##  Create followring Database as it is.....

```sql
DROP DATABASE IF EXISTS shop_practice;
CREATE DATABASE shop_practice;
USE shop_practice;


CREATE TABLE customers (
  customer_id INT AUTO_INCREMENT PRIMARY KEY,
  name        VARCHAR(50) NOT NULL,
  email       VARCHAR(120) UNIQUE
) ;


CREATE TABLE products (
  product_id INT AUTO_INCREMENT PRIMARY KEY,
  name       VARCHAR(80) NOT NULL,
  sku        VARCHAR(30) UNIQUE
) ;

CREATE TABLE orders (
  order_id    INT AUTO_INCREMENT PRIMARY KEY,
  customer_id INT,
  product_id  INT,
  quantity    INT DEFAULT 1,
  CONSTRAINT fk_orders_customer
    FOREIGN KEY (customer_id) REFERENCES customers(customer_id)
    ON DELETE RESTRICT ON UPDATE CASCADE,
  CONSTRAINT fk_orders_product
    FOREIGN KEY (product_id)  REFERENCES products(product_id)
    ON DELETE RESTRICT ON UPDATE CASCADE
);

INSERT INTO customers (name, email) VALUES
('Amit',  'amit@shop.com'),
('Bhavna','bhavna@shop.com');

INSERT INTO products (name, sku) VALUES
('Notebook A5', 'SKU-1001'),
('Blue Pen',    'SKU-2001');

INSERT INTO orders (customer_id, product_id, quantity) VALUES
(1, 1, 2),  
(2, 2, 5); 

```

### Run each SQL and observe the error. The Reason tells you which constraint is violated.

```sql 
- INSERT INTO customers (customer_id, name, email) VALUES (1, 'DuplicatePK', 'dup1@shop.com');
- INSERT INTO customers (name, email) VALUES ('Another Amit', 'amit@shop.com');
- INSERT INTO products (name, sku) VALUES ('Black Pen', 'SKU-2001');
- INSERT INTO customers (name, email) VALUES (NULL, 'nullname@shop.com');
- INSERT INTO products (name, sku) VALUES (NULL, 'SKU-3001');
- INSERT INTO orders (customer_id, product_id, quantity)VALUES (99, 1, 1);
- INSERT INTO orders (customer_id, product_id, quantity) VALUES (1, 99, 1);
- INSERT INTO orders (customer_id, product_id, quantity) VALUES (1, 99, 1);
- UPDATE orders SET customer_id = 77 WHERE order_id = 1;
- DELETE FROM customers WHERE customer_id = 1;
- DELETE FROM products WHERE product_id = 2;
```
**Note Down all the error and verify all above query** 